#                  #tuples
#            # tuples is immutable sequence of values


# tup =  (1, 2, 3, 4, "abc", 2.37)

# print(tup)
# print(len(tup))
# #print[2] = 10 # issme value add ya chage nahi kar sakte hai
# taps = (2,)   #single value nahi hoatahai tuple me agar dena hai to value ke baad , de na hoga

# saare list ke jaisa hai

              #Methods in tuples 
      # t.index(val)   => return 1st occurence index
      #t.count(val)    => count total occurences
tupl = (1, 2, 3, 3, 3, 6, 3 )
print(tupl.index(3))   # isse 3 kis index par pata chalta hai lekin 1st index sirf uske baad ka nahi
print(tupl.count(3))
